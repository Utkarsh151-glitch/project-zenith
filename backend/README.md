# Project Zenith: The Celestial Eye

FastAPI backend initialized with a clean architecture-oriented project structure.

## Setup

```bash
python -m venv .venv
pip install -r requirements.txt
cp .env.example .env
```

## Run

```bash
uvicorn app.main:app --reload
```
